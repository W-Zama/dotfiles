" システムクリップボードを使用
set clipboard=unnamedplus

" インクリメンタルサーチ有効
set incsearch

" 検索結果をハイライト
set hlsearch

" オートインデント有効
set autoindent

" K キーで改行挿入（例として `o<ESC>` を実行）
nnoremap K o<Esc>

" Insertモード中に jk でノーマルモードに戻る
inoremap jk <Esc>
